server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PavanNemani123/config-repo
          clone-on-start: true
  profiles:
    active:
    - ${spring.profiles.active}
security:
  user:
    name: root
    password: root
